name: Build and Test

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore MerchTrade.sln

      - name: Build
        run: dotnet build MerchTrade.sln --no-incremental -c Release

      - name: Test
        run: dotnet test MerchTrade.sln -c Release --no-build --verbosity normal
